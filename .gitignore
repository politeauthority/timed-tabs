node_modules
dist/
web-ext-artifacts/
*.log
.DS_Store
.env
src/dev.json   # must never exist; test seeding lives in dist/dev
backups/
docs/personal

# Python, from the scripts/ helpers. `python3 scripts/e2e-summary.py` on its own
# writes nothing, but one helper importing another does: an import compiles the
# module and leaves __pycache__ beside it.
__pycache__/
*.py[cod]
.venv/
venv/

# Development
worktrees/

# End-to-end run output (npm run e2e)
e2e-artifacts/
